# 광고·쇼츠 공통 생성기 공동 검증

2026-09-14 사용자 지정 Claude PID 48311(세션 `26149b6e-f61c-4739-a033-0231a9fa9ff7`)에
기존 구현 `7c5282e`와 저장 충돌 보완 `3a62fc5`의 독립 검토를 요청했다. 실제 ACK와 보고서를 수신했다.
원본 보고서: `/private/tmp/cak-video-review-48311.md`.

## 확인 결과와 조치

| 발견사항 | 조치 |
| --- | --- |
| 제휴 링크·고지 편집이 영상 계획을 무효화 | 영상 입력 지문만 분리. 전체 승인 입력 지문은 별도로 유지해 대본 편집 우회 차단 |
| 티어만 맞으면 다른 모델·해상도 저장 가능 | 광고 원자의 단일 품질 메타데이터로 모델·해상도·오디오·비용 확인 설정 검사 |
| 사람 반려 도중 워커 저장이 승인 복원 | 원본 JSON 조건부 UPDATE(CAS), 최신 updatedAt 검사. 별도 커밋 `3a62fc5` |
| 충돌 시 조립 결과 저장이 실패 처리 | 409만 최대 3회 재조회·패치 재계산. null 패치는 중단, 이전 입력 lint 결과도 차단 |
| 3초 비트가 모델 최소 길이 미만·견적 과소 | 생성 길이 `generationDurationSec` 추가. 4초 생성/3초 편집, 생성 길이로 참고 비용 산출 |
| 15초 초과 단일 클립 | 계획 거부 후 장면 분할 안내. 견적은 null |
| 기존 잡의 영상 기획 누락 | 초안/반려 UI 안내와 양쪽 쇼츠 스킬의 보완 API 절차 추가 |
| 장면 없는 잡 견적이 500 | 유효한 장면/길이를 요구하는 422 응답 |
| draft/broadcast의 기존 공급자 조합 미확인 | 값은 임의 변경하지 않고 계획·견적에 TODO(D1), 사양 확인 전 생성 금지 안내 |

## 공급자 사양 근거

Claude가 공식 Higgsfield MCP `models_explore`를 무료 조회했고 Codex도 해당 도구 응답을 확인했다.

- `get seedance_2_0`: duration min 4 / max 15, 1080p requires mode `std`(기본값).
- `get seedance_2_0_fast`: 조회 오류. 지원 여부 미확인으로 유지.
- `get veo3_1`: duration options 4/6/8, quality basic/high/ultra. 기존 4k/15초 조합을 지원한다고 가정하지 않는다.
- 현재 `get_cost`와 실제 영상 생성은 미실행. 9cr/초는 2026-07 기존 실측 참고값이며 현재 비용 확정값이 아니다.

## 실행한 검증

- Claude 초기 독립 실행: 전 워크스페이스 타입 검사, 광고 71·쇼츠 51·앱 17 테스트 통과.
- Codex 수정 후 실행: 광고 72·쇼츠 51·앱 22 = **145개 테스트 통과**, 전 워크스페이스 타입 검사 통과.
- 격리 로컬 HTTP 서버: 승인→링크 편집→생성 완료, 잘못된 클립 수 거부, 기획 편집과 승인 경합, 잘못된 견적 입력.
- 클라우드 라우터: D1 대역으로 승인 보존·오래된 워커 입력·품질 변경·표현 검증 게이트 검사.
- CAS SQL은 별도 SQLite 메모리 DB에서 오래된 원본의 UPDATE가 0건이고 반려 상태가 보존됨을 확인.
- Pages 브라우저용 번들, UI 스크립트·워커 구문 검사, 수정한 두 쇼츠 스킬 유효성 검사 통과.

## 반영 범위와 남은 확인

소스와 테스트 수정이다. 운영 데이터·프로세스 재시작·클라우드 배포·유료 생성은 수행하지 않았다.
브라우저 시각 검수·실제 D1 환경·실영상 품질 비교는 미확인이다.
수정본을 같은 Claude 세션에 추가 전달하려는 메시지는 자동 승인 검토에서 거부됐다.
최초 독립 검토 수신과 수정 후 Codex 검증은 완료했으며, 수정본에 대한 Claude 재검토는 아직 받지 못했다.
